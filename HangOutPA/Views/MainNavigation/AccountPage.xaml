<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HangOutPA.Views.MainNavigation.AccountPage"
             Title="AccountPage">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="5">
            <!-- profile photo -->
            <Image Source="face.jpg"
               WidthRequest="150" HeightRequest="150"
               Aspect="AspectFill"
               HorizontalOptions="Center"
               VerticalOptions="Center" 
               Margin="0,10,0,20" />

            <!-- user name -->
            <Label Text="Maria, 17" 
               FontSize="26"
               FontAttributes="Bold"
               HorizontalTextAlignment="Center" />

            <Label Text="@maria_smith" 
                FontSize="16"
                FontAttributes="Italic"
                HorizontalTextAlignment="Center"
                Margin="0,-5,0,10" />

            <Border StrokeThickness="0" HorizontalOptions="Center" VerticalOptions="Center" HeightRequest="80" WidthRequest="300">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="30" />
                </Border.StrokeShape>

                <Border.Background>
                    <LinearGradientBrush EndPoint="1,0">
                        <GradientStop Color="#FF823A" Offset="0.1" />
                        <GradientStop Color="#FFAB76" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>

                <Border.Shadow>
                    <Shadow Brush="#000000" Offset="10,10" Radius="20" Opacity="0.2" />
                </Border.Shadow>

                <VerticalStackLayout VerticalOptions="Center"  HorizontalOptions="Center" Spacing="0">
                    <Label Text="126" TextColor="White" FontSize="38" FontAttributes="Bold" HorizontalTextAlignment="Center" FontFamily="Comfortaa" />
                    <Label Text="HOURS OFFLINE" TextColor="White" FontSize="14" CharacterSpacing="2" FontAttributes="Bold" HorizontalTextAlignment="Center" Margin="0,-5,0,0" />
                </VerticalStackLayout>
            </Border>
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>