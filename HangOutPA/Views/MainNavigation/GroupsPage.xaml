<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:HangOutPA.ViewModels"
             x:Class="HangOutPA.Views.MainNavigation.GroupsPage"
             BackgroundColor="#F0F2F5"
             Shell.NavBarIsVisible="False">

    <ContentPage.BindingContext>
        <vm:GroupsViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *">

        <VerticalStackLayout Grid.Row="0" Padding="25,40,25,10">
            <Label Text="My Groups" FontSize="32" FontAttributes="Bold" TextColor="#1E293B"/>
            <Label Text="Coordinate your offline activities" TextColor="#64748B" FontSize="16"/>
        </VerticalStackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding MyGroups}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border StrokeThickness="0" BackgroundColor="White" Margin="0,0,0,15" HeightRequest="110">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="20"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="Black" Offset="5,5" Radius="10" Opacity="0.05"/>
                        </Border.Shadow>

                        <Grid ColumnDefinitions="80, *, 90" Padding="12">

                            <Border Grid.Column="0" StrokeThickness="0" WidthRequest="60" HeightRequest="60" 
                                    HorizontalOptions="Start" VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="30"/>
                                </Border.StrokeShape>
                                <Image Source="{Binding ImageUrl}" Aspect="AspectFill"/>
                            </Border>

                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="#1E293B"/>
                                <Label Text="{Binding LastMessage}" FontSize="14" TextColor="#64748B" MaxLines="1" LineBreakMode="TailTruncation"/>
                                <Label Text="{Binding LastMessageTime}" FontSize="11" TextColor="#94A3B8"/>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="2" 
                                                 VerticalOptions="Center" 
                                                 IsVisible="{Binding HasPot}" 
                                                 Spacing="4">
                                <Label Text="POT" FontSize="10" HorizontalOptions="Center" FontAttributes="Bold" TextColor="#FF823A"/>

                                <ProgressBar Progress="{Binding Progress}" 
                                             ProgressColor="#FF823A" 
                                             WidthRequest="70"
                                             HorizontalOptions="Center"/>

                                <Label Text="{Binding ProgressText}" FontSize="11" HorizontalOptions="Center" FontAttributes="Bold" TextColor="#1E293B"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>